apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Values.projectId | quote }}-frontend-ingress
  namespace: {{ .Values.namespace }}
  labels:
    projectName: {{ .Values.projectName | lower }}
    projectId: {{ .Values.projectId | quote }}
    tier: ingress
spec:
  entryPoints:
    - web
  routes:
    - match: {{ printf "Host(`%s`)" (.Values.domain) }}
      kind: Rule
      services:
        - name: {{ .Values.projectId | quote }}-frontend-service
          port: {{ .Values.app.frontend.port }}

---

apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Values.projectId | quote }}-backend-ingress
  namespace: {{ .Values.namespace }}
  labels:
    projectName: {{ .Values.projectName | lower }}
    projectId: {{ .Values.projectId | quote }}
    tier: ingress
spec:
  entryPoints:
    - web
  routes:
    - match: {{ printf "Host(`%s`)" (.Values.domain) }} && PathPrefix(`/api`)
      kind: Rule
      services:
        - name: {{ .Values.projectId | quote }}-backend-service
          port: {{ .Values.app.backend.port }}